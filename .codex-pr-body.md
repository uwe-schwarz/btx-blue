## Summary

- upgrade `@types/node` from `25.5.1` to `25.5.2`
- no code or config changes were required

## Validation

- `pnpm check`
- `pnpm test`
- `pnpm build`
- `pnpm run deps:visual -- capture --base-url http://127.0.0.1:4321 --output-dir "$ARTIFACT_ROOT/before"`
- `pnpm run deps:visual -- capture --base-url http://127.0.0.1:4321 --output-dir "$ARTIFACT_ROOT/after"`
- `pnpm run deps:visual -- compare --before-dir "$ARTIFACT_ROOT/before" --after-dir "$ARTIFACT_ROOT/after" --output-dir "$ARTIFACT_ROOT/report"`

## Visual Regression

- all five repo targets passed with `changed=0`

## Notes

- `pnpm up --latest` produced an existing peer-warning path around `typescript@6.0.2` with `astro` / `@astrojs/check`; this run did not change those packages and validation stayed green
